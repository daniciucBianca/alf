{Unregister value}

#register P 1

#if P = 1
print (text : "Print text " + P);
#else
print (text : "Print text " + P);
#endif

#unregister P

#if P = 1
print (text : "Print text " + P);
#else
print (text : "Print text " + P);
#endif
