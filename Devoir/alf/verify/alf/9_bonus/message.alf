{If then else imbricate 2}
#register P 1
#register S 2

#if P = 1
	print (text : "Print text " + P );
	#if S = 2
		print (text : "Print text " + S );
	#endif
#else
	print (text : "Print text " + P );
	#if S != 2
		print (text : "Print text " + S );
	#endif
#endif